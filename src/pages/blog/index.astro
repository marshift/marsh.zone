---
import type { MDXInstance } from "astro";
import { compareDesc } from "date-fns";
import HeaderLayout from "@/layouts/HeaderLayout.astro";
import BlogPost, { type IBlogPost } from "@/components/BlogPost.astro";
import RssLink from "@/components/RssLink.astro";

export type MDXPost = MDXInstance<IBlogPost>;
const posts = import.meta.glob<MDXPost>("./**/*.{md,mdx}", { eager: true });
---

<HeaderLayout title="the swamp" description="excerpts of my mind, to be read at your leisure.">
    <RssLink slot="head" />
    {Object.values(posts)
        .sort((a, b) => compareDesc(new Date(a.frontmatter.pubDate), new Date(b.frontmatter.pubDate)))
        .map((post, index, arr) => (<>
            <BlogPost {...post.frontmatter} href={post.url} />
            {index !== arr.length - 1 && <hr />}
        </>))
    }
</HeaderLayout>
